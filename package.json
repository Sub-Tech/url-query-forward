{
  "name": "url-query-forward",
  "version": "1.0.0",
  "description": "micro lib to grab url query string and append to links on page",
  "main": "index.js",
  "author": "Ross Matheson",
  "license": "MIT",
  "scripts": {
    "start": "babel src --watch --out-file dist/url-query-forward.min.js --presets=@babel/preset-env",
    "build": "babel src --out-file dist/url-query-forward.min.js --presets=@babel/preset-env"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.15.7",
    "@babel/core": "^7.15.5",
    "@babel/preset-env": "^7.15.6",
    "babel-loader": "^8.0.6",
    "babel-preset-minify": "^0.5.1"
  }
}
